# raw agent working data: scraped page dumps, yt-dlp output, vendor PDFs
# kept locally for auditability, not published
.sweeps/
# playwright MCP working artifacts (screenshots, snapshots)
.playwright-mcp/
